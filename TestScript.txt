local arguments_as_a_table = {...};

r = require("robot");

local moveDistance = arguments_as_a_table[0];

if moveDistance == nil then
	moveDistance = 0;
end

print("Moving " + moveDistance.toString() + " Blocks");

for i = 0, moveDistance, 1 do
	r.moveForward();
end